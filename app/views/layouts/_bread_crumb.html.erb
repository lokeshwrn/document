<div class="breadcrumbheader">
  <ul class="breadcrumb">
     <% @breadcrumb.each do |key,val| %>
        <% if val!="#" %>
            <li><a href="<%= val %>"><%= key.to_s.gsub('_',' ') %></a></li>
        <% else %>
            <li><%= key.to_s.gsub('_',' ').titlecase %></li>
        <% end %>
     <% end %>
  </ul>
</div>
