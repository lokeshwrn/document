<%if @sub_category.present?%>
  <div class="index" align="center">
    <% @sub_category.each do |a| %>
      <a href="<%= "/sub-categories/#{a.id.to_s}/articles" %>">
        <div class="article">
          <div class="head">
            <div class="title">
              <h3><%= a.name %></h3>
              <h6><%= a.description %></h6>
            </div>
            <div class="count">
          <span>
            <%= a.articles.count%>
          </span>
              <p><%= a.articles.count!=1 ? "Articles".pluralize : "Articles".singularize %></p>
            </div>
          </div>
        </div>
      </a>
    <% end %>
  </div>
<%else%>
  <div class="index" align="center">
    <h4>No Data to Display</h4>
  </div>
<%end%>